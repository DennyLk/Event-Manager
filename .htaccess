<IfModule mod_rewrite.c>
    RewriteEngine On

    # Change RewriteBase to your local project directory
    RewriteBase /LulakDP/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
